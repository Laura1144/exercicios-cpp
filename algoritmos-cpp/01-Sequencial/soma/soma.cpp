#include <iostream>
using namespace std;

int main () {
    int x, y, soma;

    cout << "Digite um valor: ";
    cin >> x;

    cout << "Digite outro valor: ";
    cin >> y;

    soma = x + y;

    cout << "A soma entre os valores e igual a " << soma << "." << endl;

    return 0;
}